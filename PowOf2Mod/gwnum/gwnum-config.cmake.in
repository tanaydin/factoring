@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(Threads)

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/gwnum-targets.cmake")

# Set variables for compatibility
set(GWNUM_FOUND TRUE)
set(GWNUM_VERSION "@PROJECT_VERSION@")
set(GWNUM_INCLUDE_DIRS "@PACKAGE_INCLUDE_INSTALL_DIR@")
set(GWNUM_LIBRARIES gwnum)

check_required_components(gwnum)
